import { useState } from 'react';
import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function EditableText({ value, onChange, as: Tag = 'span', className = '', multiline = false }) { const { hostMode } = usePortfolio(); const [editing, setEditing] = useState(false); const commit = event => { setEditing(false); onChange(event.currentTarget.innerText.trim()); }; if (!hostMode) return <Tag className={className}>{value}</Tag>; return <Tag className={`${className} editable-inline ${editing ? 'editing' : ''}`} contentEditable={editing} suppressContentEditableWarning onClick={() => setEditing(true)} onBlur={commit} onKeyDown={event => { if (!multiline && event.key === 'Enter') { event.preventDefault(); event.currentTarget.blur(); } }}>{value}</Tag>; }
