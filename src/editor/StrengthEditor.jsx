import { TextField } from './EditorField.jsx';
const newStrength = () => ({ id: crypto.randomUUID?.() || `${Date.now()}-${Math.random()}`, number: '00', title: 'New capability', description: 'Describe this capability.' });
const move = (items, index, step) => { const target = index + step; if (target < 0 || target >= items.length) return items; const next = [...items]; [next[index], next[target]] = [next[target], next[index]]; return next; };
export default function StrengthEditor({ items, updateItems }) { const update = (index, key, value) => updateItems(items.map((item, itemIndex) => itemIndex === index ? { ...item, [key]: value } : item)); return <><button className="editor-add" onClick={() => updateItems([...items, newStrength()])}>+ Add Strength</button>{items.map((item, index) => <article className="editor-item" key={item.id}><div className="editor-item-top"><strong>Capability {index + 1}</strong><span><button onClick={() => updateItems(move(items, index, -1))}>↑</button><button onClick={() => updateItems(move(items, index, 1))}>↓</button><button className="danger" onClick={() => { if (window.confirm('Delete this capability?')) updateItems(items.filter((_, itemIndex) => itemIndex !== index)); }}>Delete</button></span></div>{['number', 'title', 'description'].map(key => <TextField key={key} label={key} value={item[key]} multiline={key === 'description'} onChange={value => update(index, key, value)} />)}</article>)}</>; }
