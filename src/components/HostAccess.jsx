import { useState } from 'react';
import { supabase } from '../lib/supabase.js';
const initial={name:'YOUR NAME',role:'Software Engineer / Student',intro:'I build considered software.',email:'your.email@example.com',accent:'#c9ee6b'};
export default function HostAccess(){const [open,setOpen]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[note,setNote]=useState(''),[host,setHost]=useState(false),[form,setForm]=useState(initial);const set=(k,v)=>setForm(x=>({...x,[k]:v}));async function login(e){e.preventDefault();const {error}=await supabase.auth.signInWithPassword({email,password});if(error)return setNote('Login failed. Check your email and password.');const {data}=await supabase.from('portfolio_content').select('content').limit(1).maybeSingle();setForm({...initial,...(data?.content||{})});setHost(true)}async function save(){const {data:row}=await supabase.from('portfolio_content').select('id').limit(1).maybeSingle();const q=row?supabase.from('portfolio_content').update({content:form,updated_at:new Date().toISOString()}).eq('id',row.id):supabase.from('portfolio_content').insert({content:form});const {error}=await q;setNote(error?error.message:'Saved to your private portfolio settings.')}return <><button className="host-access" onClick={()=>setOpen(true)}>HOST ACCESS</button>{open&&<div className="host-dialog"><div>{!host?<form onSubmit={login}><button type="button" onClick={()=>setOpen(false)}>×</button><p>PRIVATE HOST ACCESS</p><h2>Manage your portfolio.</h2><input type="email" placeholder="Email" required value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password" required value={password} onChange={e=>setPassword(e.target.value)}/><button>Unlock editor ↗</button><small>{note}</small></form>:<section><button onClick={()=>setOpen(false)}>×</button><p>HOST MODE</p><h2>Profile & design</h2><label>Name<input value={form.name} onChange={e=>set('name',e.target.value)}/></label><label>Role<input value={form.role} onChange={e=>set('role',e.target.value)}/></label><label>Introduction<textarea value={form.intro} onChange={e=>set('intro',e.target.value)}/></label><label>Email<input value={form.email} onChange={e=>set('email',e.target.value)}/></label><label>Primary colour<input type="color" value={form.accent} onChange={e=>set('accent',e.target.value)}/></label><button onClick={save}>Save changes</button><small>{note}</small></section>}</div></div>}</>}
