import Reveal from './Reveal.jsx';
import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function About() { const { data } = usePortfolio(); const { profile, stats, experience, highlights } = data; return <section className="section about" id="about"><div className="section-label">01 / ABOUT</div><div className="about-layout"><Reveal className="portrait-wrap"><img src={profile.portrait} alt={`${profile.name} portrait`} /><span>PROFILE<br />IMAGE</span></Reveal><div className="about-copy"><Reveal><p className="kicker">THE ENGINEER BEHIND THE WORK</p><h2>A point of view,<br /><em>not just a stack.</em></h2><p>{profile.bio}</p></Reveal><Reveal className="about-meta"><div><small>EDUCATION</small><strong>{profile.education}</strong></div><div><small>CONTACT</small><a href={`mailto:${profile.email}`}>{profile.email} ↗</a></div></Reveal></div></div><div className="fact-strip">{stats.map(([number, label, value]) => <Reveal key={number}><article><span>{number}</span><small>{label}</small><p>{value}</p></article></Reveal>)}</div><div className="profile-notes"><div><p className="kicker">EXPERIENCE / DIRECTION</p>{experience.map(item => <article key={item.period}><span>{item.period}</span><div><h3>{item.title}</h3><p>{item.detail}</p></div></article>)}</div><div className="highlights"><p className="kicker">SIGNALS OF PROGRESS</p>{highlights.map(item => <article key={item.label}><small>{item.label}</small><p>{item.value}</p></article>)}</div></div></section>; }
