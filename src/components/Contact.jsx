import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Contact() { const { content } = usePortfolio(); const { profile, contact, sections } = content; const copy = sections.contact; return <section className="contact" id="contact"><div><p className="eyebrow">{copy.eyebrow}</p><h2>{copy.title}<br /><em>{copy.emphasis}</em></h2><a className="button" href={`mailto:${profile.email}`}>{copy.ctaLabel} <b>↗</b></a></div><footer><div><span>© {new Date().getFullYear()} {profile.name}</span><span>{profile.location}</span></div><div className="socials"><a href={`mailto:${profile.email}`}>{profile.email}</a>{contact.socialLinks.map(link => <a key={link.id} href={link.url} target="_blank" rel="noreferrer">{link.label} ↗</a>)}<a href={profile.resumeUrl}>Resume ↗</a></div></footer></section>; }
