import Reveal from './Reveal.jsx'; import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Strengths() { const { content } = usePortfolio(); const copy = content.sections.strengths; return <section className="section strengths" id="strengths"><div className="section-label">{copy.label}</div><Reveal className="strengths-intro"><h2>{copy.title}<br /><em>{copy.emphasis}</em></h2><p>{copy.description}</p></Reveal><div className="strength-grid">{content.strengths.map(item => <Reveal key={item.id}><article><span>{item.number}</span><h3>{item.title}</h3><p>{item.description}</p><b>↗</b></article></Reveal>)}</div></section>; }
