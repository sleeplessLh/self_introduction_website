import { strengths } from '../data/portfolio.js';
import Reveal from './Reveal.jsx';
export default function Strengths() { return <section className="section strengths" id="strengths"><div className="section-label">03 / CAPABILITIES</div><Reveal className="strengths-intro"><h2>Useful in the<br /><em>important places.</em></h2><p>Not a rating system. A working set of strengths that evolves with each problem worth solving.</p></Reveal><div className="strength-grid">{strengths.map(([number, title, description]) => <Reveal key={number}><article><span>{number}</span><h3>{title}</h3><p>{description}</p><b>↗</b></article></Reveal>)}</div></section>; }
