import { useState } from 'react';
import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Navbar() { const [open, setOpen] = useState(false); const { content } = usePortfolio(); return <header className="navbar"><a className="brand" href="#home" aria-label="Home">{content.profile.initials}<i>.</i></a><nav className={open ? 'open' : ''} aria-label="Main navigation">{content.navigation.map(link => <a key={link.id} href={`#${link.id}`} onClick={() => setOpen(false)}>{link.label}</a>)}</nav><a className="nav-contact" href={`mailto:${content.profile.email}`}>{content.hero.primaryLabel} <span>↗</span></a><button className="menu-button" onClick={() => setOpen(!open)} aria-label="Toggle menu" aria-expanded={open}><span /><span /></button></header>; }
