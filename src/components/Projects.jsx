import { useState } from 'react'; import Reveal from './Reveal.jsx'; import CollectionCard from './CollectionCard.jsx'; import DetailModal from './DetailModal.jsx'; import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Projects() { const { content } = usePortfolio(); const [selected, setSelected] = useState(null); const [expanded, setExpanded] = useState(false); const copy = content.sections.projects; const visible = expanded ? content.projects : content.projects.slice(0, 3); return <section className="projects-section compact-collection" id="projects"><div className="projects-inner"><div className="section-label">{copy.label}</div><Reveal className="projects-heading"><h2>{copy.title}<br /><em>{copy.emphasis}</em></h2><p>{copy.description}</p></Reveal>{content.projects.length ? <><div className="compact-list">{visible.map(item => <Reveal key={item.id}><CollectionCard item={item} type="project" detailLabel={copy.detailLabel} onOpen={() => setSelected(item)} /></Reveal>)}</div>{content.projects.length > 3 && <button className="collection-toggle" onClick={() => setExpanded(value => !value)}>{expanded ? copy.collapseLabel : copy.expandLabel} <b>{expanded ? '↑' : '↓'}</b></button>}</> : <p className="empty-collection">{copy.emptyLabel}</p>}</div>{selected && <DetailModal item={selected} type="project" onClose={() => setSelected(null)} />}</section>; }
