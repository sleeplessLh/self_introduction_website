export default function CollectionCard({ item, type, onOpen, detailLabel }) { const labels = item.technologies || item.tags || []; return <article className="collection-card"><button className="collection-cover" onClick={onOpen} aria-label={`Open ${item.title}`}><img src={item.coverImage} alt="" loading="lazy" /><span>{item.date}</span></button><div className="collection-info"><p className="project-category">{item.label}</p><h3>{item.title}</h3><p className="collection-description">{item.description}</p>{type === 'competition' && <p className="achievement"><b>Achievement</b><span>{item.achievement || 'Add achievement or participation result'}</span></p>}<div className="collection-meta"><button className="gallery-link" onClick={onOpen}>Gallery {item.gallery?.length ? `(${item.gallery.length})` : ''} ↗</button><div className="collection-tags">{labels.map(label => <span key={label}>{label}</span>)}</div></div>{type === 'project' && <div className="collection-links">{item.github && <a href={item.github} target="_blank" rel="noreferrer">GitHub ↗</a>}{item.liveDemo && <a href={item.liveDemo} target="_blank" rel="noreferrer">Demo ↗</a>}{!item.github && !item.liveDemo && <button className="card-open" onClick={onOpen}>{detailLabel} ↗</button>}</div>}</div></article>; }
