export default function CollectionCard({ item, onOpen, detailLabel }) { return <article className="collection-card"><button className="collection-cover" onClick={onOpen} aria-label={`Open ${item.title}`}><img src={item.coverImage} alt="" loading="lazy" /><span>{item.date}</span></button><div><p className="project-category">{item.label}</p><h3>{item.title}</h3><p>{item.description}</p><button className="card-open" onClick={onOpen}>{detailLabel} ↗</button></div></article>; }
