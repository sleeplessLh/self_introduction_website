import { useState } from 'react';
export default function DetailModal({ item, type, onClose }) { const [image, setImage] = useState(item.coverImage); const gallery = [item.coverImage, ...(item.gallery || [])].filter(Boolean); const tags = item.technologies || item.tags || []; return <div className="detail-modal" role="dialog" aria-modal="true" aria-label={item.title}><button onClick={onClose} aria-label="Close">×</button><div className="detail-image"><img src={image} alt={item.title} /></div><div className="detail-copy"><p className="kicker">{item.label} · {item.date}</p><h2>{item.title}</h2><p>{item.description}</p><div className="detail-tags">{tags.map(tag => <span key={tag}>{tag}</span>)}</div>{type === 'project' && <div className="detail-links">{item.github && <a href={item.github} target="_blank" rel="noreferrer">GitHub ↗</a>}{item.liveDemo && <a href={item.liveDemo} target="_blank" rel="noreferrer">Live demo ↗</a>}</div>}<div className="gallery">{gallery.map((src, index) => <button key={`${src}-${index}`} onClick={() => setImage(src)}><img src={src} alt={`${item.title} gallery ${index + 1}`} /></button>)}</div></div></div>; }
