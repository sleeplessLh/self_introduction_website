import { useState } from 'react'; import Reveal from './Reveal.jsx'; import CollectionCard from './CollectionCard.jsx'; import DetailModal from './DetailModal.jsx'; import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Competitions() { const { content } = usePortfolio(); const [selected, setSelected] = useState(null); const copy = content.sections.competitions; return <section className="section collection-section" id="competitions"><div className="section-label">{copy.label}</div><Reveal className="projects-heading"><h2>{copy.title}<br /><em>{copy.emphasis}</em></h2><p>{copy.description}</p></Reveal>{content.competitions.length ? <div className="collection-grid">{content.competitions.map(item => <CollectionCard key={item.id} item={item} detailLabel={copy.detailLabel} onOpen={() => setSelected(item)} />)}</div> : <p className="empty-collection">{copy.emptyLabel}</p>}{selected && <DetailModal item={selected} type="competition" onClose={() => setSelected(null)} />}</section>; }
