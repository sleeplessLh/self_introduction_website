import { useState } from 'react'; import Reveal from './Reveal.jsx'; import CollectionCard from './CollectionCard.jsx'; import DetailModal from './DetailModal.jsx'; import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Competitions() { const { content } = usePortfolio(); const [selected, setSelected] = useState(null); const [expanded, setExpanded] = useState(false); const copy = content.sections.competitions; const visible = expanded ? content.competitions : content.competitions.slice(0, 3); return <section className="section collection-section compact-collection" id="competitions"><div className="section-label">{copy.label}</div><Reveal className="projects-heading"><h2>{copy.title}<br /><em>{copy.emphasis}</em></h2><p>{copy.description}</p></Reveal>{content.competitions.length ? <><div className="compact-list">{visible.map(item => <Reveal key={item.id}><CollectionCard item={item} type="competition" detailLabel={copy.detailLabel} onOpen={() => setSelected(item)} /></Reveal>)}</div>{content.competitions.length > 3 && <button className="collection-toggle" onClick={() => setExpanded(value => !value)}>{expanded ? copy.collapseLabel : copy.expandLabel} <b>{expanded ? '↑' : '↓'}</b></button>}</> : <p className="empty-collection">{copy.emptyLabel}</p>}{selected && <DetailModal item={selected} type="competition" onClose={() => setSelected(null)} />}</section>; }
