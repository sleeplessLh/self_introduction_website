import Reveal from './Reveal.jsx'; import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function LearningJourney() { const { content } = usePortfolio(); const copy = content.sections.learning; return <section className="learning-section" id="learning"><div className="section"><div className="section-label">{copy.label}</div><Reveal className="projects-heading"><h2>{copy.title}<br /><em>{copy.emphasis}</em></h2><p>{copy.description}</p></Reveal>{content.learningJourney.length ? <div className="learning-list">{content.learningJourney.map(item => <article key={item.id}><div><span>{item.status}</span><small>{item.period}</small></div><h3>{item.title}</h3><p className="project-category">{item.category}</p><p>{item.summary}</p><div>{[...item.topics, ...item.technologies].map(tag => <i key={tag}>{tag}</i>)}</div></article>)}</div> : <p className="empty-collection">{copy.emptyLabel}</p>}</div></section>; }
