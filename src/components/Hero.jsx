import stargazing from '../assets/images/hero-stargazing.png';
import { usePortfolio } from '../context/PortfolioContext.jsx';
export default function Hero() { const { content } = usePortfolio(); const { hero, profile } = content; return <section className="hero" id="home">{hero.videoSrc ? <video className="hero-video" autoPlay muted loop playsInline src={hero.videoSrc} /> : <div className="hero-video hero-placeholder" style={{ backgroundImage: `url(${stargazing})` }} aria-label="Illustrated night sky with a person and dog" />}<div className="hero-stars" aria-hidden="true" /><div className="hero-overlay" /><div className="hero-glow" /><div className="hero-content"><p className="eyebrow">{hero.eyebrow}</p><h1>{hero.title} <em>{hero.emphasis}</em></h1><p className="hero-intro">{hero.intro}</p><div className="hero-actions"><a className="button" href={`mailto:${profile.email}`}>{hero.primaryLabel} <b>↗</b></a><a className="text-link" href="#projects">{hero.secondaryLabel} <b>↓</b></a></div></div><p className="scroll-cue">{hero.scrollLabel} <i /></p></section>; }
